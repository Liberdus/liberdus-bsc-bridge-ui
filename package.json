{
  "name": "liberdus-bsc-bridge-ui",
  "version": "1.0.0",
  "description": "Liberdus BSC Bridge UI",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "jsdom": "^27.0.1",
    "vitest": "^3.2.4"
  }
}
